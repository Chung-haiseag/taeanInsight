# 태안 크롤러 환경변수 — /etc/taean-crawler.env 로 복사 후 값 채우기 (chmod 600).
# 값 = Worker 시크릿과 동일해야 함.

# Worker GOV_IMPORT_TOKEN 과 동일한 값 (군청·CCTV 적재 인증)
TAEAN_GOV_TOKEN=

# ITS 국가교통정보센터 인증키 (도로 CCTV)
ITS_API_KEY=

# 적재 대상 Worker API (기본값 사용 시 생략 가능)
TAEAN_API=https://taean-insight-api.chs9182.workers.dev
